/* tslint:disable:no-unused-variable */

import { TestBed, inject, waitForAsync } from '@angular/core/testing';
import { AboutRepertoireResolverService } from './about-repertoire-resolver.service';

describe('Service: AboutRepertoireResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AboutRepertoireResolverService]
    });
  });

  it('should ...', inject([AboutRepertoireResolverService], (service: AboutRepertoireResolverService) => {
    expect(service).toBeTruthy();
  }));
});
